{"title": "calcWaterYear doesn't work with NA values", "body": "This throws an error:\n```r\n# date vector w/ NAs\nmydates <- seq(as.POSIXct('2010-01-01'), as.POSIXct('2011-01-01'), by='day')\nmydates <- c(as.Date(NA), mydates, as.Date(NA))\n\ncalcWaterYear(mydates)\n\nError in waterYear[whichPastOct] <- calYear[whichPastOct] + 1 :\n  NAs are not allowed in subscripted assignments\n```", "labels": ["bug"]}
